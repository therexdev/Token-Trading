{
  "name": "trade-koinos",
  "version": "1.0.0",
  "private": true,
  "description": "Trade Koinos — orderbook DEX frontend plus the Google sign-in bridge (app.tradekoinos.com)",
  "main": "server/server.js",
  "scripts": {
    "build": "npm --prefix frontend ci --include=dev && npm --prefix frontend run build",
    "start": "node server/server.js",
    "dev": "node server/server.js",
    "postinstall": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
